- name: install nginx
  hosts: dynamic_frontend
  become: yes
  tasks:
  - name: Install nginx into dynamic inventory frontend servers
    anisble.builtin.package:
      name: nginx
      state: present